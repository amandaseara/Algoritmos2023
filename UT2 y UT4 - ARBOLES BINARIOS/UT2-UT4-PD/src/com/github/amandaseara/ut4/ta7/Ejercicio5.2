#EJERCICIO 5.2 

Modifica el algoritmo del ejercicio 5.1 para que no siga recorriendo una vez que se encontró el 
elemento buscado.

encuentraLinealDos(nombreAtributo, valorAtributo): devuelve verdadero si hay un nodo con 
esa etiqueta, y falso en caso contrario.

RESPUESTA: El algoritmo desarrollado en el ejercicio 5.1 ya cumple con esto.
